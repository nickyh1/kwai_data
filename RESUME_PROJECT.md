# 简历项目描述（浓缩版）

---

## 快手小店数据同步服务

**技术栈**：Java 17 · Spring Boot · MongoDB · 快手商家开放平台 SDK · Maven

**项目描述**：
基于 Spring Boot 开发的快手小店 ERP 数据底座服务，通过快手开放平台 API 定时自动拉取多店铺的订单、资金流水、提现记录及店铺信息，持久化至 MongoDB，为业务分析与对账提供统一数据支撑。

**核心工作**：
- 接入快手商家开放平台 SDK，实现订单游标分页拉取、未结算订单批量同步及提现流水查询，单次同步覆盖近一个月数据；
- 设计 Facade + Service 分层架构，支持多店铺并行同步，通过 `ShopAuthRegistry` 统一管理各店铺凭证；
- 实现 Token 自动刷新机制（每 24 小时定时执行），结合 MongoDB 持久化，保障长期免运维稳定运行；
- 数据同步任务每 3 小时自动触发，采用防死循环的游标翻页策略（最大页数限制 + cursor 变化检测），确保大数据量下的稳定性。
